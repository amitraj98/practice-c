// 21. Write a C program that reads an integer and checks the specified range to which it belongs. Print an error message if the number is negative and greater than 80. 
// Test Data :
// Input an integer: 15
// Expected Output:
// Range [0, 20]

#include<stdio.h>
int main(){
    
    int integer;
    int range;
    
    printf("input the integer :" );
    scanf("%d", &integer);
    
    if(integer>=0 && integer<=20){
        printf("Range [0, 20]");
    }else if(integer >= 21 && integer <= 40){
        printf("Range [21, 40]");
    }else if(integer >= 41 && integer <= 60){
        printf("Range [41, 60]");
    }else if(integer >= 61 && integer <= 80){
        printf("Range [61, 80]");
    }else {
        printf("outside the range");
    }
    
    return 0;
}
