// 11.a Write a program in C to make such a pattern like right angle triangle with a number which will repeat a number in a row. 
// The pattern like :
//  5
//  55
//  555
//  5555
//  55555

#include <stdio.h>
int main(){
    
    int i,j;
    int n;
    
    printf("display the pattern like right angle triangle with a number which will repeat a number in a row = ");
    scanf("%d", &n);
    
    for(i=1; i<=n; i++){
        for(j=1; j<=i; j++)
        printf("%d",n); //when we use n
        printf("\n");
    }
   
    return 0;
}