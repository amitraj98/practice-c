// 41 (a) Write a C program to print 3 numbers on a line, starting with 0 and printing n lines. Accept the number of lines (n, integer) from the user.
// Enter the number of lines: 5
// 0 1 2 
// 1 2 3 
// 2 3 4 
// 3 4 5 
// 4 5 6 

#include <stdio.h>

int main() 
{
    int n, i, j, num = 1;

    printf("Enter the number of lines: ");
    scanf("%d", &n);

    for (i = 0; i < n; i++) 
    {
            printf("%d %d %d ", i,i+1,i+2); 
            num++; 
      printf("\n");
    }

    return 0;
}
